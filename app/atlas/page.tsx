import AtlasView from "@/components/AtlasView";

export const metadata = { title: "舞台マップ — MANGA MAP" };

// ページ全体がマップ（見出しと説明はマップ上のオーバーレイに置いた）
export default function AtlasPage() {
  return <AtlasView lang="ja" />;
}
