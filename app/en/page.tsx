import GenreView from "@/components/GenreView";

export const metadata = { title: "MANGA MAP — A Map of Manga History & Genres" };

export default function EnHome() {
  return <GenreView lang="en" />;
}
